import Link from "next/link";
import { Badge, Card, LinkButton } from "@/components/ui";
import { getAffiliateLinks } from "@/lib/content";

export default async function AdminAffiliateLinksPage({ searchParams }: { searchParams: Promise<{ saved?: string }> }) {
  const [links, query] = await Promise.all([getAffiliateLinks(), searchParams]);
  return <div><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-extrabold text-white">Affiliate links</h1><p className="mt-2 text-sm text-slate-400">One stable ID can be reused in posts and calculator recommendations.</p></div><LinkButton href="/admin/affiliate-links/new" variant="azure">New affiliate link</LinkButton></div>{query.saved && <p className="mt-5 rounded-xl border border-(--color-success-border) bg-(--color-success-soft) p-4 text-sm text-(--color-success-text)">Affiliate registry saved.</p>}<div className="mt-8 space-y-4">{links.map((link) => <Card key={link.id} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><Badge variant={link.enabled ? "mint" : "default"}>{link.enabled ? "enabled" : "disabled"}</Badge><span className="font-mono text-xs text-slate-500">{link.id}</span></div><h2 className="mt-3 font-bold text-white">{link.provider}</h2><p className="mt-1 max-w-2xl truncate text-xs text-slate-500">{link.affiliateUrl || link.destinationUrl}</p></div><Link href={`/admin/affiliate-links/${link.id}/edit`} className="rounded-lg bg-(--color-brand) px-3 py-2 text-center text-xs font-bold text-white">Edit</Link></Card>)}</div></div>;
}
